var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Overview","page":"API reference","text":"","title":"Overview"},{"category":"section","location":"api/#Loading-data","page":"API reference","text":"","title":"Loading data"},{"category":"section","location":"api/#Scenario-encoding","page":"API reference","text":"","title":"Scenario encoding"},{"category":"section","location":"api/#Scoring","page":"API reference","text":"","title":"Scoring"},{"category":"section","location":"api/#Editing-a-loaded-model","page":"API reference","text":"Both matrices are edited in place, so a model can be re-analysed without re-parsing its .scw file — which is what makes sensitivity sweeps cheap.","title":"Editing a loaded model"},{"category":"section","location":"api/#Succession-rules","page":"API reference","text":"The succession dynamics is pluggable: define a struct MyRule <: SuccessionRule and a single succession_step(::MyRule, cib, u) method, and every analysis routine works with it through a generic path.","title":"Succession rules"},{"category":"section","location":"api/#Dynamics","page":"API reference","text":"","title":"Dynamics"},{"category":"section","location":"api/#Very-large-scenario-spaces","page":"API reference","text":"For spaces beyond the table method's memory (see the README's \"Very large scenario spaces\" for the decision table): exact streaming lives on find_basins itself (method=:stream, signature_range, cache_bytes); share estimation and structural decomposition are their own entry points.","title":"Very large scenario spaces"},{"category":"section","location":"api/#Levers-between-futures","page":"API reference","text":"Basin sizes measure volume; this measures connectivity — which commitment moves the system from one consistent scenario into another, and where the current state of the world drifts if nothing is done. Every walk starts from a known attractor, so the cost does not depend on the size of the scenario space at all.","title":"Levers between futures"},{"category":"module","location":"api/#CrossImpactBalances","page":"API reference","text":"CrossImpactBalances\n\nCross-Impact Balance (CIB) scenario analysis. A Julia implementation of the methodology in Weimer-Jehle (2006), ported from the Python sei-international/cibsa library with a two-phase basin-of-attraction analysis, a threaded incremental exhaustive sweep, and an exact branch-and-bound search that prunes provably-inconsistent regions of the scenario space. The succession dynamics can be changed through SuccessionRule. There is a default called GlobalSuccession which follows the standard ScenarioWizard rule.\n\nReference\n\nWeimer-Jehle, W. (2006). Cross-impact balances: A system-theoretical approach to cross-impact analysis. Technological Forecasting and Social Change, 73(4), 334–361.\n\n\n\n\n\n","title":"CrossImpactBalances"},{"category":"function","location":"api/#CrossImpactBalances.load_scw","page":"API reference","text":"load_scw(scw_file; sl_file=nothing, kernel=nothing, algorithm=:auto) -> CIB\n\nParse a ScenarioWizard .scw file and optionally a .sl solutions file. Returns a fully populated CIB object.\n\nUnless a kernel or an sl_file is supplied, the kernel is computed by find_consistent, which always searches the full scenario space and uses every available thread (start Julia with julia -t auto). algorithm is forwarded to select the search strategy.\n\nFor a model whose scenario space exceeds typemax(Int64) that search is branch-and-bound with nothing to fall back to, so loading can take a while rather than failing fast. Pass kernel=Vector{Vector{Int}}() to load the matrix without searching at all.\n\n\n\n\n\n","title":"CrossImpactBalances.load_scw"},{"category":"function","location":"api/#CrossImpactBalances.load_solutions","page":"API reference","text":"load_solutions(cib::CIB, sl_file::String) -> Vector{Vector{Int}}\n\nParse a ScenarioWizard .sl solutions file. Returns 0-based variant indices.\n\n\n\n\n\n","title":"CrossImpactBalances.load_solutions"},{"category":"type","location":"api/#CrossImpactBalances.CIB","page":"API reference","text":"CIB\n\nCross-impact balance analysis object.\n\nFields:\n\ndescriptors: ordered list of descriptor names\nvariants: dict mapping descriptor name to list of variant names\nnumberOfVariants: number of variants per descriptor (can be different for each descriptor)\ncim: the cross-impact matrix (n × n), n = sum of all variants. Row r holds the impacts contributed by variant r onto every other variant.\ncim_t: the transpose of cim — cim_t[j, r] == cim[r, j]. Stored alongside cim so that \"sum a fixed-row vector across many j\" hot loops (impactbalance, findbasins) can iterate contiguously through column-major storage, which unlocks SIMD. Construction cost is one permutedims at load time.\nnumberOfDimensions: total number of variants (size of CIM)\nnumberOfDescriptors: number of descriptors\nconsistentScenarios: list of consistent scenarios (each a Vector{Int}, 0-based variant indices)\ndesc_offsets: cumulative 0-based variant offsets per descriptor, shows where in the CIM each descriptor's variants start\n\nA struct in Julia is immutable: the field bindings can never be reassigned after construction (like a C# readonly record or a frozen dataclass). The arrays the fields point at can still have their contents changed — which is how consistentScenarios gets filled in after the object is built.\n\n\n\n\n\n","title":"CrossImpactBalances.CIB"},{"category":"function","location":"api/#CrossImpactBalances.signature","page":"API reference","text":"signature(cib, u) -> Int\n\nCompute a unique, sequential, integer signature for scenario u (0-based variant indices).\n\nA scenario is a choice of one variant per descriptor. Treating those choices as the digits of a mixed-radix number (each descriptor's \"digit\" can count up to its own variant count) maps every scenario to a distinct integer in 0:max_signature(cib) — exactly like reading [7, 2, 4] as a decimal number, except each position can have a different base. This lets the search code step through all scenarios with a single counter and avoids expensive modulus operations\n\n\n\n\n\n","title":"CrossImpactBalances.signature"},{"category":"function","location":"api/#CrossImpactBalances.inv_signature","page":"API reference","text":"inv_signature(cib, s) -> Vector{Int}\n\nConvert a signature back to a scenario (0-based variant indices), the reverse of the function above\n\n\n\n\n\n","title":"CrossImpactBalances.inv_signature"},{"category":"function","location":"api/#CrossImpactBalances.max_signature","page":"API reference","text":"max_signature(cib) -> Int\n\nThe maximum signature value (= total scenarios - 1).\n\nThrows an ArgumentError for models with more than typemax(Int64) ≈ 9.2×10¹⁸ scenarios (for scale: ~63 binary or ~40 ternary descriptors), because signatures beyond that cannot be represented and every signature-driven analysis — the odometer sweep, find_basins — would silently wrap. Before this guard, max_signature itself wrapped silently.\n\nWhat survives past the line is what never numbers a scenario. scenario_count counts the space exactly. find_consistent still finds the kernel exactly: its branch-and-bound builds scenarios one descriptor at a time and orders the result by comparing variant choices, so only algorithm=:sweep is refused there. estimate_basins still estimates basin shares. influence_structure and fix_descriptor cut the model into exactly analysable pieces.\n\n\n\n\n\n","title":"CrossImpactBalances.max_signature"},{"category":"function","location":"api/#CrossImpactBalances.scenario_count","page":"API reference","text":"scenario_count(cib) -> Int128\n\nThe total number of scenarios in the model — the product of every descriptor's variant count.\n\nReturned as an Int128 deliberately. The product of even modest variant counts outruns Int64 at around forty ternary descriptors, and worse, Int64 arithmetic wraps silently when it overflows — a model would quietly report a garbage size. (It also outruns Float64 exactness far earlier, at 2⁵³ ≈ 9×10¹⁵: real matrices exist whose scenario count displayed through a double comes out with its final digits rounded.) Int128 is exact out to ~1.7×10³⁸, which no conceivable CIB matrix approaches, and this function guards even that.\n\nThis is the number to display; max_signature (= this minus one, as an Int) is the number to iterate to, and it throws for models whose signatures no longer fit in Int64 — see its docstring for what still works in that regime.\n\n\n\n\n\n","title":"CrossImpactBalances.scenario_count"},{"category":"function","location":"api/#CrossImpactBalances.impact_balance","page":"API reference","text":"impact_balance(cib, u) -> Vector{Int}\n\nCompute the impact balance vector for scenario u: one score per variant across all descriptors (length numberOfDimensions). The score of a variant is the total impact the scenario's chosen variants exert on it — the higher the score, the better that variant fits the scenario.\n\n\n\n\n\n","title":"CrossImpactBalances.impact_balance"},{"category":"function","location":"api/#CrossImpactBalances.set_impact!","page":"API reference","text":"set_impact!(cib, src_desc, src_var, tgt_desc, tgt_var, value) -> Int\n\nSet the cross-impact contributed by the source variant (src_desc=src_var) onto the target variant (tgt_desc=tgt_var), i.e. cim[source, target], and return the previous value.\n\nDescriptors and variants may be given by name (AbstractString) or by 0-based index (Integer). This updates both the cross-impact matrix cim and its stored transpose cim_t, so that every analysis routine — which may read either — sees a consistent matrix.\n\nBecause the matrices are edited in place, the already-loaded model changes without any re-parse of the .scw file; the next find_consistent or find_basins call reflects the new value. That is what makes it cheap to sweep one expert judgement across a range and watch the consistent scenarios move.\n\nNote the ! in the name: by Julia convention it warns that the argument is modified rather than copied.\n\n\n\n\n\n","title":"CrossImpactBalances.set_impact!"},{"category":"function","location":"api/#CrossImpactBalances.get_impact","page":"API reference","text":"get_impact(cib, src_desc, src_var, tgt_desc, tgt_var) -> Int\n\nReturn the current cross-impact cim[source, target] contributed by the source variant (src_desc=src_var) onto the target variant (tgt_desc=tgt_var). Descriptors and variants may be given by name or 0-based index. The read-only partner of set_impact!.\n\n\n\n\n\n","title":"CrossImpactBalances.get_impact"},{"category":"type","location":"api/#CrossImpactBalances.SuccessionRule","page":"API reference","text":"SuccessionRule\n\nSuccession is one of the two core concepts of CIB — \"how do we move from one scenario to the next\". The other is consistency — \"is there a better scenario than this one\". These are separate: whether a better scenario exists is a different question from how you would get there.\n\nTo allow flexibility in how succession is calculated, we define an abstract supertype SuccessionRule. An abstract type cannot be instantiated itself — it only serves as a parent for concrete rule types, the way an interface or abstract base class would in C# or Python.\n\nTo add a new rule, define\n\nstruct MyRule <: SuccessionRule end\n\n(<: means \"is a subtype of\") and a single method\n\nsuccession_step(rule::MyRule, cib::CIB, u::Vector{Int}) -> Vector{Int}\n\nand every analysis routine — find_consistent and find_basins — works with it immediately, because they dispatch on the rule's type. The only assumption is that a rule depends solely on the current scenario, not on the path taken to reach it (in technical terms, that the process is Markov).\n\n\n\n\n\n","title":"CrossImpactBalances.SuccessionRule"},{"category":"type","location":"api/#CrossImpactBalances.GlobalSuccession","page":"API reference","text":"The standard succession rule, as used by ScenarioWizard and the original CIBSA.\n\nEvery variant is scored against the current scenario, and then every descriptor moves at once to its best-scoring variant. All the moves are decided from the same snapshot of scores, so within a step no descriptor can see what any other descriptor did. Ties favour the current variant, then the lowest variant index.\n\n\n\n\n\n","title":"CrossImpactBalances.GlobalSuccession"},{"category":"type","location":"api/#CrossImpactBalances.SequentialSuccession","page":"API reference","text":"Sequential (successive / Gauss–Seidel) succession: descriptors move one at a time, in descriptor order, and the scores are recomputed after each move. So descriptor 2 chooses in full knowledge of what descriptor 1 just did, within the same step.\n\nIts trajectories differ from GlobalSuccession — where an unstable scenario goes next depends on the update order — but its consistent scenarios are exactly the same ones. See fixed_point_margin for why that matters.\n\n\n\n\n\n","title":"CrossImpactBalances.SequentialSuccession"},{"category":"function","location":"api/#CrossImpactBalances.fixed_point_margin","page":"API reference","text":"fixed_point_margin(rule) -> Union{Nothing,Int}\n\nDeclares that a rule's consistent scenarios can be identified by a simple local test, and how demanding that test is.\n\nOrdinarily a scenario is consistent when no descriptor could do better by switching variant. Declaring a margin m relaxes this to \"no descriptor could do better by more than m\". That reflects the fact that real social systems have inertia: they do not reorganise themselves over a trivial improvement. A margin of 0 is the ordinary meaning, and both built-in rules declare it.\n\nThe declaration is a promise, not a calculation — the rule is asserting that its consistent scenarios are exactly the scenarios passing that test. Making the promise is what buys the rule the fast searches in find_consistent. A rule that returns nothing (the default for any new rule) makes no promise and is searched the slow, always-correct way.\n\nThe name uses \"fixed point\" — the dynamical-systems term for a consistent scenario, i.e. one whose succession step maps it to itself. The two are the same thing; see the vocabulary note at the top of CrossImpactBalances.jl.\n\n\n\n\n\n","title":"CrossImpactBalances.fixed_point_margin"},{"category":"function","location":"api/#CrossImpactBalances.succession_step","page":"API reference","text":"succession_step(cib, u) -> Vector{Int}\nsuccession_step(rule, cib, u) -> Vector{Int}\n\nTake one step of succession from scenario u and return the scenario it leads to. Omitting rule means GlobalSuccession.\n\nA scenario that steps to itself is a consistent scenario: nobody wants to move, so nothing changes.\n\n\n\n\n\n","title":"CrossImpactBalances.succession_step"},{"category":"function","location":"api/#CrossImpactBalances.find_consistent","page":"API reference","text":"find_consistent(cib; rule=GlobalSuccession(), algorithm=:auto,\n                bnb_node_budget=nothing) -> Vector{Vector{Int}}\n\nFind every consistent scenario in the model — the model's kernel.\n\nrule selects the succession dynamics (SuccessionRule; default GlobalSuccession).\n\nalgorithm selects the search strategy. It only applies to rules that declare a fixed_point_margin; any other rule is searched one scenario at a time and passing an algorithm is an error.\n\n:sweep — check every scenario, using the incremental odometer walk in sweep.jl.\n:bnb   — branch-and-bound (branch_and_bound.jl): build scenarios one descriptor at a time and discard whole families that provably contain nothing consistent. Exact — it returns the identical answer — and usually looks at a small fraction of the scenarios.\n:auto  (default) — the sweep for small models (under 100,000 scenarios, where branch-and-bound's overheads are not repaid), otherwise branch-and-bound with a budget; if the pruning turns out too weak to pay off, the budget trips and the sweep runs instead.\n\nBranch-and-bound never numbers a scenario, so it — and therefore :auto above 100,000 scenarios — keeps working past typemax(Int64) ≈ 9.2×10¹⁸ scenarios, where signatures stop existing (max_signature). Two strategies do not cross that line, and both say so plainly: :sweep, which walks signatures in order, and the generic scan used by rules that declare no fixed_point_margin. Nor is there anything to fall back TO if branch-and-bound exhausts an explicitly-set bnb_node_budget there — so :auto does not set one: its usual budget of a sixteenth of the scenario count is unreachable at that scale, and the search simply runs to completion.\n\nThe result is ordered by ascending signature — decided by comparing the scenarios' variant choices from the last descriptor down, which is the same order and needs no signature to exist. So it does not depend on how many threads happened to be running, at any model size.\n\n\n\n\n\n","title":"CrossImpactBalances.find_consistent"},{"category":"function","location":"api/#CrossImpactBalances.find_basins","page":"API reference","text":"find_basins(cib; rule=GlobalSuccession(), method=:auto,\n            signature_range=nothing, cache_bytes=2^30, progress=false)\n    -> (fixed_points, basin_sizes, cycle_count)\n\nExhaustive basin-of-attraction analysis under rule (default GlobalSuccession). Follows the succession chain from every scenario in the space, counting how many starting points converge to each consistent scenario.\n\nReturns three things: the consistent scenarios found, how many scenarios drain into each one, and how many scenarios instead end up in a repeating cycle and so belong to no basin. (A \"fixed point\" here is the same thing as a consistent scenario — see the note above.) The result is exact whichever method computes it, and identical between them.\n\nmethod picks how:\n\n:table — the two-phase design: a threaded sweep fills a flat successor table (every scenario's succession step), then a resolution pass walks the table with path compression so each scenario is resolved exactly once. For example, the first phase works out that scenario A maps to B, B maps to C, and C maps to D which is a consistent scenario; therefore A, B, C and D are all in D's basin, and the second phase works this out. Fastest, but its memory is two flat tables over the whole space — the successor table at 4 bytes per scenario when every signature fits Int32, 8 otherwise, plus a 4-byte label table.\n:stream — walks every start to its trajectory's end with per-attractor counters and NO tables (see stream.jl): memory stays flat however large the space, at the price of recomputing successions along each walk — CPU-days for spaces around 10¹²–10¹³, so measure first with bench/stream_calibration.jl. signature_range=a:b restricts the starts to that range so the space can be split across machines (scripts/basin_stream_worker.jl / scripts/basin_stream_merge.jl); the sum(sizes) + cycles == total invariant then holds per range, and the merged ranges reproduce the full-space result exactly. cache_bytes sizes the lossy memo cache that lets walks stop on already-resolved territory (0 disables it); progress=true prints a throttled status line to stderr.\n:auto (default) — :table when both tables fit this machine's memory, otherwise an error explaining the choices (estimate_basins for shares with error bars at any size, method=:stream for exact-but-slow, influence_structure/product_basins for exact-by-decomposition). Deliberately never silently starts a multi-day stream.\n\nThe default GlobalSuccession rule takes a fast path in both methods: the odometer walk maintains the impact balance incrementally (one row delta per scenario, no allocation) instead of calling succession_step per scenario. Any other rule uses the generic per-scenario path — identical output, just slower.\n\n\n\n\n\n","title":"CrossImpactBalances.find_basins"},{"category":"function","location":"api/#CrossImpactBalances.estimate_basins","page":"API reference","text":"estimate_basins(cib; rule=GlobalSuccession(), samples=1_000_000,\n                seed=0xC1BBA512C1BBA512, confidence=0.95, kernel=nothing)\n    -> BasinEstimate\n\nEstimate each consistent scenario's basin SHARE by uniform random sampling — the analysis for scenario spaces beyond every exact method's reach (they run out around 10¹³; this does not care). Each of samples starting scenarios is drawn uniformly (every scenario in the space equally likely), walked to its end under rule, and tallied; shares come back with Wilson confidence intervals at level confidence.\n\nThis estimates; it never discovers. The kernel is taken exact — from kernel if given, else the model's already-computed consistent scenarios, else find_consistent (whose branch-and-bound handles huge spaces) — and every consistent scenario appears in the result even with zero hits, carrying an explicit upper bound on its share rather than a false zero. A share the sampler cannot see (much below ~1/samples) is reported exactly that way. For rules that declare no fixed_point_margin and supply no kernel, the kernel cannot be precomputed at this scale; the estimate then reports only what sampling reaches, and says so.\n\nThe result is a pure function of (model, rule, samples, seed, confidence) — independent of thread count, machine, and Julia version (the generator is the package's own; see rng.jl). Same seed, same estimate, bit for bit.\n\nSee find_basins for exact analysis when the space allows it, and influence_structure / product_basins for exact analysis of huge models that decompose.\n\n\n\n\n\n","title":"CrossImpactBalances.estimate_basins"},{"category":"type","location":"api/#CrossImpactBalances.BasinEstimate","page":"API reference","text":"BasinEstimate\n\nThe result of estimate_basins: estimated basin shares with confidence intervals, for a kernel that is known exactly.\n\nParallel vectors, sorted by ascending attractor signature:\n\nfixedPoints: every attractor in the report — the exact kernel supplied or computed up front, plus any fixed point the sampler reached (there should be none of the latter if the kernel was complete)\nhits: how many sampled starts drained into each (0 is meaningful: the attractor exists, and its share was too small to see at this sample size)\nshares: hits / samples\nciLow, ciHigh: the Wilson interval for each share at level confidence\nsizeEstimates: shares × scenarioCount, as Float64 — at these scales a size estimate is meaningful to its leading digits only\n\nAnd the remainder of the tally:\n\ncycleHits, cycleShare, cycleCiLow, cycleCiHigh: starts that ended in a repeating cycle (no basin), as count and interval\nsamples, seed, confidence: the estimate's defining inputs\nscenarioCount::Int128: the exact size of the space (scenario_count)\n\nsum(hits) + cycleHits == samples always holds — the sampled twin of the exact analysis' sum(sizes) + cycles == total invariant.\n\n\n\n\n\n","title":"CrossImpactBalances.BasinEstimate"},{"category":"function","location":"api/#CrossImpactBalances.influence_structure","page":"API reference","text":"influence_structure(cib) -> InfluenceStructure\n\nCompute the influence map: which descriptors actually influence which (see the file header), the independent islands it splits into, and the dial / jump-and-stay descriptors. Cost is one pass over the matrix — trivial even for models whose scenario space is astronomically large, which is exactly when it matters: the map is what decides whether product_basins or fix_descriptor can make an impossible exact analysis possible.\n\n\n\n\n\n","title":"CrossImpactBalances.influence_structure"},{"category":"type","location":"api/#CrossImpactBalances.InfluenceStructure","page":"API reference","text":"InfluenceStructure\n\nThe influence map of a model, from influence_structure. Descriptors are identified by 1-based position in cib.descriptors (the display shows names).\n\ndescriptors: the names, for printing\nactiveEdges: activeEdges[i, j] is true when descriptor i has a real say over j — some two variants of i score j's variants differently. The diagonal is a descriptor's say over itself (nonstandard matrices have one).\ncomponents: the islands — groups of descriptors with no influence in or out, each sorted; singletons included. One island = no decomposition.\ntieFrozen: dials — descriptors nothing influences and whose constant scores are level, so under the tie rule they never move from where they start. Candidates for fix_descriptor slicing.\nforced: jump-and-stays — (descriptor, maximizers) pairs: nothing influences the descriptor but its constant scores have winners, so after one step it sits at a maximizer forever (0-based variant indices; every consistent scenario must hold one of them).\n\n\n\n\n\n","title":"CrossImpactBalances.InfluenceStructure"},{"category":"function","location":"api/#CrossImpactBalances.fix_descriptor","page":"API reference","text":"fix_descriptor(cib, descriptor, variant) -> CIB\n\nA new model with descriptor pinned to variant: the descriptor keeps its place but retains only that one variant (the engine treats one-variant descriptors as never moving), and every judgment row and column belonging to its other variants is dropped. Both may be given by name or 0-based index, as in get_impact.\n\nThe pinned model's scenario space is the original's sliced at that setting — smaller by the descriptor's variant count — and its dynamics are exactly the original's restricted to that slice when nothing gave the pinned descriptor a reason to move (it was a dial — see InfluenceStructure — or the slice is simply the question being asked). Two uses:\n\nConditional analysis: \"hold Trade at Protectionist and show me the consistent scenarios / basins of everything else.\" Cheap to build, so sweeping a judgment or a setting across its range is a loop, not a re-parse.\nExact decomposition: pin a dial to each of its variants in turn and run the pinned analyses side by side; pin everything outside an island to reduce the model to that island (split_cib does this).\n\nThe result is a complete, ordinary CIB (kernel left empty — the original's consistent scenarios use the unpinned space's numbering) and works with every analysis in the package.\n\n\n\n\n\n","title":"CrossImpactBalances.fix_descriptor"},{"category":"function","location":"api/#CrossImpactBalances.split_cib","page":"API reference","text":"split_cib(cib; structure=influence_structure(cib)) -> Vector{CIB}\n\nOne model per island of the influence map: island k's model is the original with every descriptor OUTSIDE the island pinned (fix_descriptor) to its first variant. Pinning is exact here because no outside descriptor has any say over the island — its rows all agree on the island's columns, so WHICH variant stays makes no difference to the island's scores; the one kept row carries the (variant-independent) contribution those descriptors were always making. Descriptor positions are unchanged, so an island model's scenarios read directly as the island's part of a full scenario.\n\n\n\n\n\n","title":"CrossImpactBalances.split_cib"},{"category":"function","location":"api/#CrossImpactBalances.product_basins","page":"API reference","text":"product_basins(cib; rule=GlobalSuccession(), method=:auto,\n               structure=influence_structure(cib), cache_bytes=2^30,\n               progress=false) -> ComposedBasins\n\nExact basins for a model whose influence map splits into independent islands: run find_basins on each island's model (split_cib) and compose — island results multiply, because a start drains to a combination of island attractors exactly when each of its island parts drains to its own (see the file header). With islands of sizes n₁, n₂, … this prices an exact analysis of the n₁·n₂·… -scenario model at the cost of the largest island — the only exact route once the full space passes ~10¹³.\n\nComposition is proven for the built-in rules, whose steps act on each island separately; a custom rule is refused rather than guessed about. With a single island this degenerates to plain find_basins (in Int128 clothing) — the result is then exactly as unreachable as before, which the influence map will already have told you. method, cache_bytes and progress are forwarded to each island's find_basins.\n\n\n\n\n\n","title":"CrossImpactBalances.product_basins"},{"category":"type","location":"api/#CrossImpactBalances.ComposedBasins","page":"API reference","text":"ComposedBasins\n\nExact basin analysis of a decomposable model, from product_basins — the same information as find_basins' tuple, in Int128 because composed counts live at scales Int64 cannot hold.\n\nfixedPoints, basinSizes: every consistent scenario of the FULL model (one combination per choice of island consistent scenarios), sorted by ascending signature, with its exact full-space basin size\ncycleCount: full-space starts that end in a cycle (some island cycling)\nscenarioCount: the full space, so sum(basinSizes) + cycleCount == scenarioCount\ncomponents: the islands (1-based descriptor positions), and componentResults: each island's own (fixedPoints, basinSizes, cycleCount) exactly as find_basins returned it\n\n\n\n\n\n","title":"CrossImpactBalances.ComposedBasins"},{"category":"function","location":"api/#CrossImpactBalances.transition_graph","page":"API reference","text":"transition_graph(cib; rule=GlobalSuccession(), kernel=nothing,\n                 from=nothing, radius=1) -> TransitionGraph\n\nMap the levers between a model's futures: from every consistent scenario, try changing each descriptor to each of its other variants, follow succession to wherever that leads, and record which commitments move the system where.\n\nfrom adds the current state of the world as an extra node — a scenario that need not be consistent. Its first edge is the baseline: where the world drifts with no commitment at all. The rest are the commitments that redirect it. Give it either a Vector{Int} of 0-based variant indices, one per descriptor, or a Dict naming every descriptor:\n\ntransition_graph(cib; from = Dict(\"Trade\" => \"Protectionist\",\n                                  \"Economy\" => \"Low\", ...))\n\nEvery descriptor must be assigned — a half-specified world state has no well-defined trajectory, so it is an error rather than a guess.\n\nradius=2 additionally tries every pair of descriptors, reporting a pair only when it reaches a destination no single change reached — the combination levers. Cost per source node is Σ(mᵢ-1) single commitments and about (Σ(mᵢ-1))² / 2 pairs, for variant counts mᵢ; nothing here scales with the size of the scenario space, so this analysis runs identically on a model of thirty scenarios and one of 10¹⁸.\n\nkernel supplies the consistent scenarios instead of computing them (they are taken exactly, as in estimate_basins; an empty kernel plus a from gives a world-only graph). Use to_dot to render the result.\n\n\n\n\n\n","title":"CrossImpactBalances.transition_graph"},{"category":"type","location":"api/#CrossImpactBalances.TransitionGraph","page":"API reference","text":"TransitionGraph\n\nThe lever map of a model, from transition_graph.\n\nNodes (parallel vectors nodes, kinds, signatures, sorted by ascending signature, with the world node — if any — last):\n\nkinds[i] is :attractor for a consistent scenario, :cycle for a repeating loop reached by some commitment (named by its lowest-signature member, so every walk entering that loop names it identically), or :world for the supplied current state of the world.\nsignatures are Int128, so node identity survives models whose scenario count passes Int64 (see scenario_count).\n\nEdges: edges, a Vector{Transition} ordered by source node, then by the order the commitments were tried (baseline first, then single descriptors, then pairs).\n\nRobustness (per node, counting single-descriptor commitments only, so the numbers mean the same thing whatever radius was used):\n\nreturned[i] — commitments after which the system came back to where it started. moved[i] — commitments that landed on a different consistent scenario. destabilized[i] — commitments that landed in a cycle.\nrobustness[i] = returned / total, i.e. the fraction of single changes this future absorbs. NaN marks a node that was never perturbed — a cycle, or a fixed point discovered as somebody's destination rather than supplied in the kernel. Those nodes are destinations only.\n\nworldIndex is the world node's position, or 0 when no from= was given.\n\n\n\n\n\n","title":"CrossImpactBalances.TransitionGraph"},{"category":"type","location":"api/#CrossImpactBalances.Transition","page":"API reference","text":"Transition\n\nOne edge of a TransitionGraph: the commitment changes, applied to node from, leads the system to node to.\n\nchanges: the commitment, as (descriptor, variant) pairs — descriptor 1-based (its position in cib.descriptors), variant 0-based (as scenarios are numbered everywhere in this package). One pair for a single-descriptor lever, two for a pair lever, and empty for the world node's baseline edge, which is where the current state of the world drifts on its own.\nsteps: succession steps the walk computed on the way. A rough measure of how far the destination was, not a path length — the walker counts its cycle canonicalisation lap too (see walk.jl).\n\n\n\n\n\n","title":"CrossImpactBalances.Transition"},{"category":"function","location":"api/#CrossImpactBalances.to_dot","page":"API reference","text":"to_dot(graph) -> String\n\nRender a TransitionGraph as a Graphviz digraph, ready to paste into any Graphviz viewer (dot -Tsvg levers.dot -o levers.svg, or one of the browser-based renderers).\n\nConsistent scenarios are boxes labelled with their variant names and robustness; cycles are dashed ellipses; the current state of the world is a heavy box. Each edge carries the commitment that causes the transition, so the picture reads directly as \"change this, end up there\". Written by hand rather than through a graph library — the package has no dependencies.\n\n\n\n\n\n","title":"CrossImpactBalances.to_dot"},{"category":"section","location":"#CrossImpactBalances.jl","page":"Home","text":"Julia implementation of Cross-Impact Balance (CIB) scenario analysis — a methodology for finding the internally consistent futures of a qualitative multi-descriptor system from an expert-elicited cross-impact matrix.\n\nThis package is a reimplementation of the Python sei-international/cibsa library with a memoized basin-of-attraction analysis and a threaded exhaustive search. The benchmark suite (test/benchmark.jl) routinely reports 25–100× speedups over the Python reference.","title":"CrossImpactBalances.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url=\"https://github.com/IainDM/CrossImpactBalances.jl\")\n\nfind_consistent and find_basins always search the whole scenario space. For multi-threaded search, start Julia with multiple threads:\n\njulia -t auto --project=.","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using CrossImpactBalances\n\ncib = load_scw(\"path/to/CIB_global.scw\")\n\n# Each consistent scenario\nfor u in cib.consistentScenarios\n    println(\"sig=\", signature(cib, u))\n    for (i, desc) in enumerate(cib.descriptors)\n        println(\"  \", desc, \" = \", cib.variants[desc][u[i] + 1])\n    end\nend\n\n# Exhaustive basin-of-attraction analysis\nfixed_points, basin_sizes, cycle_count = find_basins(cib)\n\nFor more, see the examples/ folder in the repository and the API reference.","title":"Quick start"},{"category":"section","location":"#Citation","page":"Home","text":"@article{weimer-jehle-2006-cib,\n  author  = {Weimer-Jehle, Wolfgang},\n  title   = {Cross-impact balances: A system-theoretical approach to cross-impact analysis},\n  journal = {Technological Forecasting and Social Change},\n  volume  = {73},\n  number  = {4},\n  pages   = {334--361},\n  year    = {2006},\n}","title":"Citation"}]
}
